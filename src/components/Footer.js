// Define a functional component called 'Footer'
const Footer = () => {
    // The component returns an h3 element that displays "My Footer in another component"
    return <h3>My Footer in another component</h3>;
  };
  // Export the 'Footer' component so it can be imported and used in other files
  export default Footer;