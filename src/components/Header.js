// Define a functional component called 'Footer'
const Header = () => {
     // The component returns an h1 element that displays "My Header in another component"
    return <h1>My Header in another component</h1>;
  };
  // Export the 'Footer' component so it can be imported and used in other files
  export default Header;